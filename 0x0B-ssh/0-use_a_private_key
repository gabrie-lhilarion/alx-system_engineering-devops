#!/usr/bin/env bash

# Define variables for server hostname or IP address, username, and private key file
private_key=~/.ssh/school
user=ubuntu
server=your_server_ip

# SSH connection command
ssh -i "$private_key" "$user"@"$server"